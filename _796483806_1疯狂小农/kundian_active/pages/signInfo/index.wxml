<view class="sing_wrapper">
    <view class="sing_item" wx:for="{{sign}}" wx:key="id">
        <image mode="widthFix" src="{{item.avatarurl}}"></image>
        <view>{{item.nickname}}</view>
    </view>
</view>
